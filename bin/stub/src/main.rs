compile_error!("To use this package, you'll need to run the compiler on the target OS, with the compiler tooling, and select the kernel module you want. Bailing!")

fn main() {
    unimplemented!();
}
